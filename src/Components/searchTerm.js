import React from 'react';


const SearchTerm = ({termss}) => {
    return (
        <div>
            <h1>
                Showing Result for: {termss}
            </h1>
        </div>
    );
};

export default SearchTerm;